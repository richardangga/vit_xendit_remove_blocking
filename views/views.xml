<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vit_xendit_remove_blocking.list">
      <field name="name">vit_xendit_remove_blocking list</field>
      <field name="model">vit_xendit_remove_blocking.vit_xendit_remove_blocking</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vit_xendit_remove_blocking.action_window">
      <field name="name">vit_xendit_remove_blocking window</field>
      <field name="res_model">vit_xendit_remove_blocking.vit_xendit_remove_blocking</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vit_xendit_remove_blocking.action_server">
      <field name="name">vit_xendit_remove_blocking server</field>
      <field name="model_id" ref="model_vit_xendit_remove_blocking_vit_xendit_remove_blocking"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vit_xendit_remove_blocking" id="vit_xendit_remove_blocking.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vit_xendit_remove_blocking.menu_1" parent="vit_xendit_remove_blocking.menu_root"/>
    <menuitem name="Menu 2" id="vit_xendit_remove_blocking.menu_2" parent="vit_xendit_remove_blocking.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vit_xendit_remove_blocking.menu_1_list" parent="vit_xendit_remove_blocking.menu_1"
              action="vit_xendit_remove_blocking.action_window"/>
    <menuitem name="Server to list" id="vit_xendit_remove_blocking" parent="vit_xendit_remove_blocking.menu_2"
              action="vit_xendit_remove_blocking.action_server"/>
    -->
  </data>
</odoo>